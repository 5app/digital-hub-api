root: users
query:
    fields:
      'Costcenter': 'costcenter'
      'AssetID': 'session.events.ref_id'
      'Asset': 'session.events.asset.name'
    format: csv
    groupby:
      - costcenter
      - session.events.ref_id
    orderby:
      - costcenter DESC
      - session.events.ref_id DESC
    limit: 10000
    filter:
      session:
        events:
          created_time: '2018'
          action: 'open'
          category: 'asset'
